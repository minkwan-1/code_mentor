import { Box } from "@mui/material";

const Footer = () => {
  return (
    <Box
      sx={{
        width: "100%",
        borderTop: "1px solid gray",
        height: "100px",
      }}
    >
      푸터
    </Box>
  );
};

export default Footer;
